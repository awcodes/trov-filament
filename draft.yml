models:
  Page:
    title: string
    slug: string unique
    status: string default:draft
    hero_image: string nullable
    hero_content: mediumtext nullable
    content: longtext nullable
    seo_title: string
    seo_description: text
    indexable: boolean default:true
    has_chat: boolean default:true

seeders: Page
